<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="concept_vzn_twt_4q">
 <title>Previewing Multiple Stages</title>
 <shortdesc>You can preview data for a set of stages within a pipeline. Preview multiple stages to
  verify that the set of stages processes data as expected. </shortdesc>
 <conbody>
  <p>When you preview multiple stages, you select the first stage and the last stage in the set. The
   Preview panel then displays the input data and the output data for the set of stages. That is,
   the panel displays the input data for the first stage in the set, and the output data for the
   last stage in the set. </p>
  <p>For example, the following screenshot displays a preview for the set of stages from &lt;stage
   name> to &lt;stage name><draft-comment author="Loretta">get a screenshot, describe more.
   </draft-comment></p>
 </conbody>
</concept>
